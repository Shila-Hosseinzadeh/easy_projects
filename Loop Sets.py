# Loop Sets : in sets only have for not while
# for
myset = {"a", "b", "c", "d","e","f" }
print(f"myset: {myset}")
for i in myset:
    print(f" myset items are: {i}") # \
    # items not in order. we have different order in every for.\
    # because in every run we have a different my set. \
    # for chooses every loop in every different myset
